{
  "name": "@ouzx-me/api",
  "type": "module",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy --minify --env production",
    "preview": "wrangler deploy --minify --env preview",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest run",
    "lint": "eslint --fix .",
    "typecheck": "tsc --noEmit",
    "typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@ouzx-me/logger": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@ouzx-me/eslint-config": "workspace:*",
    "@ouzx-me/typescript-config": "workspace:*",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
